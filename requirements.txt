aiogram>=3.15,<4
pydantic-settings>=2.7

# data layer
SQLAlchemy[asyncio]>=2.0.36
alembic>=1.14
aiosqlite>=0.20          # free default: a local file database
asyncpg>=0.30            # when you move to Postgres / Supabase

# free stack (defaults)
google-genai>=1.0        # dialogue + assessment, free tier
edge-tts>=7.0            # speech synthesis, no API key
openai>=1.59             # speech recognition via Groq's OpenAI-compatible API

# optional paid providers
anthropic>=1.0           # LLM_PROVIDER=anthropic
httpx>=0.27              # TTS_PROVIDER=elevenlabs

# optional infra
redis>=5.2
cachetools>=5.5
